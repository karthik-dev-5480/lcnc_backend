package com.lcncbe.controller;

import org.springframework.web.bind.annotation.*;

import com.lcncbe.dto.PageRequest;

import org.springframework.http.ResponseEntity;
import java.util.Map;

@RestController
@RequestMapping("/api/pages")
@CrossOrigin(origins = "http://localhost:3000") // Quick way to enable CORS for testing
public class PageController {

    @PostMapping
    public ResponseEntity<?> createPage(@RequestBody PageRequest request) {
        // Business Logic
        System.out.println("Type: " + request.type());
        System.out.println("Name: " + request.name());

        return ResponseEntity.ok(Map.of(
            "status", "success",
            "message", request.type() + " successfully created"
        ));
    }
}
